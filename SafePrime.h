#pragma once
#include <iostream>
#include <cstring>
#include <math.h>
#define ll long long
 

using std::cout;
using std::cin;

ll f(ll a, ll b, ll c);
ll fermat(ll p, ll a);
//bool isSafePrime(ll x, ll a);